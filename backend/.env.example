# Copier ce fichier en .env et remplir les valeurs
# Ne pas committer .env (déjà dans .gitignore)

# Django
SECRET_KEY=votre-cle-secrete-production
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,votre-domaine.up.railway.app

# Base de données (Railway fournit DATABASE_URL automatiquement)
# Pour le développement local avec PostgreSQL :
# DB_NAME=eschool_db
# DB_USER=postgres
# DB_PASSWORD=postgres
# DB_HOST=localhost
# DB_PORT=5432

# CORS - URL du frontend en production
CORS_ALLOWED_ORIGINS=https://votre-frontend.up.railway.app

# Celery / Redis (optionnel)
# CELERY_BROKER_URL=redis://localhost:6379/0
# CELERY_RESULT_BACKEND=redis://localhost:6379/0

# Stripe
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=

# Twilio (SMS/WhatsApp)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# Email
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=noreply@eschool.rdc
